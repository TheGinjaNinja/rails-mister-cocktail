<!-- <div class="container">
  <div class="row mb-3">
    <h1>Try our delicious cocktails</h1>
  </div>
    <div class="row mb-3">
      <% @cocktails.each do |cocktail| %>
        <div class="col mb-3">
          <div class="card" style="width: 18rem">
            <div class="card-img-top" style="background-image: url('<%= cl_image_path cocktail.photo.key %>')">
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= link_to cocktail.name, cocktail_path(cocktail) %></h5>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <div class="row mb-3">
    <div class="btn btn-primary">
      <%= link_to "Create a new cocktail", new_cocktail_path, :class => "btn btn-primary" %>
    </div>
  </div>
</div> -->


<div class="container">
  <div class="cards">
    <% @cocktails.each do |cocktail| %>
    <%= link_to cocktail_path(cocktail) do%>
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path cocktail.photo.key %>')">
        <div>
          <h5><%= cocktail.name %></h5>
        </div>
      </div>
      <% end %>
    <% end %>
  </div>
</div>
